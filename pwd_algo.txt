

* password hashing
  
  argon2
  catena
  lyra2
  makwa
  yescrypt
  
* symmetric

  rc6
  serpent
  aes
  twofish
  
* asymmetric 

  rsa
  ntru

  
* password authentication
  
  srp
  
  
* random number generator

  fortuna
  
pwd1 -> scrypt() -> fpu() to generate tables

random order encryption

2MB tables are generated using FPU functions, 1 for each algorithm

rc6
aes
serpent
twofish

based on the input password hash from scrypt, 4 bits are selected
which will then mix the order in which algorithm is selected.

encrypt tables using pwd2 scrypt() + rc6()
encrypt tables using pwd3 scrypt() + serpent()
encrypt tables using pwd4 scrypt() + twofish()
encrypt tables using pwd4 scrypt() + aes()

pwd2 -> encrypt() -> scrypt() -> serpent256() -> rc6() ->